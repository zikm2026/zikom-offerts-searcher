# Server Configuration
NODE_ENV=development
PORT=3000

# API Configuration
API_VERSION=v1

# Security
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=24h

# Database (Docker: uruchom `docker compose up -d`, potem `npx prisma migrate deploy`)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/zikom

# CORS
CORS_ORIGIN=http://localhost:3000

# Email Configuration (home.pl: host IMAP weź z Panelu: Poczta → Opcje skrzynki → Serwery pocztowe, np. serwer123.home.pl)
EMAIL_USER=twoj-email@domena.pl
EMAIL_PASSWORD=twoje-haslo
EMAIL_HOST=serwer123.home.pl
EMAIL_PORT=993
EMAIL_TLS=true
EMAIL_CHECK_INTERVAL=60000

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp
